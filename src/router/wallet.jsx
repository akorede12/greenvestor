import { } from 'react';

export default function ({ children }) {
    // implement the wallet configuration here
    // your wagmiClient goes here, set the variable to client for easy identification

    return /** replace this tag with your WagmiConfig tag */ <>
        {children}
    </>;
};