// export all the components that are under the card forlder
// e.g export { default as $<card_name>} from './<card_folder_name>'

export { default as $project } from './project';
export { default as $portfolio } from './portfolio';
